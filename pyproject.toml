# The contents of this file is based on https://github.com/home-assistant/core/blob/dev/pyproject.toml

[tool.ruff]
target-version = "py313"  # the oldest Python a supported Home Assistant runs on

[tool.ruff.lint]
# Turned on a few groups at a time, each with its findings fixed; "ALL"
# (Home Assistant core's setting) is where this is heading.
select = [
    "A",    # flake8-builtins
    "ASYNC", # async
    "B",    # flake8-bugbear
    "BLE",  # blind except
    "C4",   # flake8-comprehensions
    "C90",  # mccabe complexity, max-complexity below
    "DTZ",  # datetimes with a time zone
    "E",    # pycodestyle errors
    "EM",   # exception messages
    "ERA",  # commented-out code
    "EXE",  # executable files
    "FLY",  # f-strings
    "FURB", # refurb
    "F",    # pyflakes
    "FBT",  # boolean positional arguments
    "G",    # logging format
    "I",    # isort
    "ICN",  # import conventions
    "INP",  # implicit namespace packages
    "ISC",  # implicit string concatenation
    "LOG",  # logging
    "N",    # pep8-naming
    "PERF", # performance
    "PGH",  # pygrep hooks
    "PIE",  # flake8-pie
    # pylint: imports at the top, named values; the complexity limits follow
    "PLC",
    "PLE",
    "PLR2004",
    "PLW",
    "PT",   # pytest style
    "PTH",  # pathlib
    "RET",  # return
    "RUF",  # ruff
    "S",    # bandit
    "SIM",  # flake8-simplify
    "SLF",  # private member access
    "T20",  # print
    "TRY",  # tryceratops
    "TID",  # tidy imports
    "UP",   # pyupgrade
    "W",    # pycodestyle warnings
    "YTT",  # sys.version
]

ignore = [
    # Home Assistant integrations import their own modules relatively
    "TID252",
    # Arrows, dashes and multiplication signs in text are written on purpose
    "RUF001", "RUF002", "RUF003",
    # noqa for rule groups not switched on yet (PLC0415, SLF001); drop this
    # once they are
    "RUF100",
    "ARG001", # "unused-argument"
    "D202", # no-blank-line-after-function
    "D203", # no-blank-line-before-class (incompatible with formatter)
    "D212", # multi-line-summary-first-line (incompatible with formatter)
    "E501",   # line too long
    "COM812", # incompatible with formatter
    "ISC001", # incompatible with formatter
    # Moving imports into type-checking blocks can mess with pytest.patch()
    "TC001", # Move application import {} into a type-checking block
    "TC002", # Move third-party import {} into a type-checking block
    "TC003", # Move standard library import {} into a type-checking block

    "TRY003", # Avoid specifying long messages outside the exception class
    "TRY400", # Use `logging.exception` instead of `logging.error`

    # May conflict with the formatter, https://docs.astral.sh/ruff/formatter/#conflicting-lint-rules
    "W191",
    "E111",
    "E114",
    "E117",
    "D206",
    "D300",
    "Q",
    "COM812",
    "COM819",

    # Disabled because ruff does not understand type of __all__ generated by a function
    "PLE0605"
]

[tool.ruff.lint.flake8-pytest-style]
fixture-parentheses = false

[tool.ruff.lint.pyupgrade]
keep-runtime-typing = true

[tool.ruff.lint.mccabe]
max-complexity = 25

[tool.ruff.lint.per-file-ignores]
"scripts/**/*.py" = [
    "T201", # command-line tools print their output
    "INP001", # standalone scripts, not a package
]
"tests/**/*.py" = [
    # at least this three should be fine in tests:
    "S101", # asserts allowed in tests...
    "ANN001",
    "ARG", # Unused function args -> fixtures nevertheless are functionally relevant...
    "FBT", # Don't care about booleans as positional arguments in tests, e.g. via @pytest.mark.parametrize()
    # The below are debateable
    "PLR2004", # Magic value used in comparison, ...
    "S311", # Standard pseudo-random generators are not suitable for cryptographic purposes
    "SLF001", # tests look at the private state they check
    "DTZ001", # tests build the unit's zone-less wall-clock readings on purpose
    "S104", # tests bind servers to 0.0.0.0 as the integration does
    "S108", # /tmp paths in mocks are never written
    "S603", # tests run the repository's own scripts
]
